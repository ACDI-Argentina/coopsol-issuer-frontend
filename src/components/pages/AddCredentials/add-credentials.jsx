import React from 'react';
import './_style.scss';

import api from '../../../services/api-calls/all';

// const { endpoint } = api();

const AddCredentials = props => {
  return <div className="Credentials">AddCredentials</div>;
};

export default AddCredentials;
