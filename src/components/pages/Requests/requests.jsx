import React from 'react';
import './_style.scss';

import api from '../../../services/api-calls/all';

// const { endpoint } = api();

const Requests = () => {
  return <div className="Credentials">Requests Page</div>;
};

export default Requests;
