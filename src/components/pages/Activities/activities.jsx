import React from 'react';
import './_style.scss';

import api from '../../../services/api-calls/all';

// const { endpoint } = api();

const Activities = () => {
  return <div className="Activities">Activities Page</div>;
};

export default Activities;
